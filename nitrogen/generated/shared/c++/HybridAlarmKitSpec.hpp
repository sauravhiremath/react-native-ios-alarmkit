///
/// HybridAlarmKitSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <NitroModules/Promise.hpp>
#include <functional>

namespace margelo::nitro::alarmkit {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `AlarmKit`
   * Inherit this class to create instances of `HybridAlarmKitSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridAlarmKit: public HybridAlarmKitSpec {
   * public:
   *   HybridAlarmKit(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridAlarmKitSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridAlarmKitSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridAlarmKitSpec() override = default;

    public:
      // Properties
      virtual bool getIsSupported() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::string>> getAuthorizationState() = 0;
      virtual std::shared_ptr<Promise<bool>> requestAuthorization() = 0;
      virtual std::shared_ptr<Promise<void>> schedule(const std::string& id, const std::string& configJson) = 0;
      virtual std::shared_ptr<Promise<void>> cancel(const std::string& id) = 0;
      virtual std::shared_ptr<Promise<void>> stop(const std::string& id) = 0;
      virtual std::shared_ptr<Promise<void>> pause(const std::string& id) = 0;
      virtual std::shared_ptr<Promise<void>> resume(const std::string& id) = 0;
      virtual std::shared_ptr<Promise<void>> countdown(const std::string& id) = 0;
      virtual std::shared_ptr<Promise<std::string>> getAlarms() = 0;
      virtual std::string addAlarmsListener(const std::function<void(const std::string& /* alarmsJson */)>& callback) = 0;
      virtual void removeAlarmsListener(const std::string& subscriptionId) = 0;
      virtual std::string addAuthorizationListener(const std::function<void(const std::string& /* state */)>& callback) = 0;
      virtual void removeAuthorizationListener(const std::string& subscriptionId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "AlarmKit";
  };

} // namespace margelo::nitro::alarmkit
